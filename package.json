{
  "name": "typescript-di-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Code-generation TypeScript dependency injection framework, modeled after Dagger 2.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
